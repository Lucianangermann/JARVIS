# Optional voice stack — install with:
#     pip install -r requirements-voice.txt
#
# Whisper drags in torch (~2 GB) and numba/llvmlite, which sometimes needs
# system LLVM to build. If llvmlite fails on macOS, install LLVM first:
#     brew install llvm
# Then retry.

openai-whisper>=20231117
# Optional but strongly recommended: 2-3× faster Whisper backend.
# server/stt.py auto-detects and uses it if installed. With this you can
# bump WHISPER_MODEL to large-v3-turbo and still beat the vanilla `small`
# latency. Pulls in CTranslate2 (~50 MB).
faster-whisper>=1.0
pyttsx3>=2.90
sounddevice>=0.4.7
numpy>=1.26
scipy>=1.13

# --- Speaker verification for the security layer (voice_auth) ---
# resemblyzer = local GE2E speaker embeddings (~17 MB model, CPU-only).
# Reuses the torch that whisper already pulled in, so no extra GB here.
resemblyzer>=0.1.4
# resemblyzer → webrtcvad still imports the legacy `pkg_resources`, which
# setuptools removed in 81. Pin below that so the import doesn't break.
setuptools<81
